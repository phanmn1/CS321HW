int global = foo();

int foo(int num, boolean hah)
{
   ;
   print test(blah, blah, blah);

   return foo(int num);
// shouldn't be able to pass an 'int' anything as the expression to a function call.
}
